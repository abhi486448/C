#include<stdio.h>
#include<string.h>

int main(){
    // char name[100];
    
    // fgets(name,100,stdin);
    // int length=strlen(name);
    
    // printf("no. of chrater : %d ",length);


    char arr[100]="rana";
    char str[100]="rancho";
    // strcpy(arr,str);
    // puts(arr);

    // puts(strcat(arr,str));

    char b[]="b";
    char a[]="a";
    
    printf("%d",strcmp(a,b));

    return 0;
}